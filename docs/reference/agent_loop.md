# agent_loop

Основной цикл агента и инструменты (`list_dir`, `read_file`,
`propose_write`, `api_call`).

::: harness.agent_loop
    options:
      members:
        - HarnessAgent
        - _escape_attr
        - _redact_args
        - _wrap_tool_result
      show_root_heading: false
